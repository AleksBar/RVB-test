export default function preloader() {
    // const bodyClass = document.querySelector('body').classList;
    // const preloaderClass = document.querySelector('.preloader').classList;
    // window.addEventListener('load', function() {
    //     setTimeout(() => {
    //         if (!(preloaderClass.contains('preloader_loaded'))){
    //             preloaderClass.add('preloader_loaded');
    //         }
    //         setTimeout(() => {
    //             if (bodyClass.contains('body-block')){
    //                 bodyClass.remove('body-block');
    //             }
    //         }, 50);
    //     }, 1000);
    // });
}